apiVersion: 1

datasources:
  - name: PostgreSQL
    type: postgres
    access: proxy
    url: db:5432
    database: postgres
    user: postgres
    secureJsonData:
      password: example
    isDefault: true
    editable: true
